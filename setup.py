from setuptools import setup, find_packages

setup(
    name="GidNET",
    version="0.0.1",
    packages=find_packages(include=['gidnet', 'utils']),
    install_requires=[],
    # Add more package metadata if needed
)
